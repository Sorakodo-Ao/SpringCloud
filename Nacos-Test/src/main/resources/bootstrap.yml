spring:
  application:
    name: nacos-config
#    name: sorako
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml #指定yaml格式的配置
#      config:
#        server-addr: localhost:8848
#        file-extension: yaml #指定yaml格式的配置
#        namespace: Sorako_namespace
#        group: SORAKO_GROUP
      discovery:
        server-addr: localhost:8848